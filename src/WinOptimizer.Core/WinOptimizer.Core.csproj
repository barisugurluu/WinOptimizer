<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>WinOptimizer.Core</RootNamespace>
    <Description>Çekirdek modeller, arayüzler ve enum'lar — IOptimizationModule sözleşmesi.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- WindowsVersionInfo.MapEditionToProOrHigher saf/test edilebilir tutuldu
         (Safety'deki desenle aynı). -->
    <InternalsVisibleTo Include="WinOptimizer.Core.Tests" />
  </ItemGroup>

</Project>
